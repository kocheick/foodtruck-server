package com.shevapro.services

//import com.shevapro.data.repository.usersplaces.UsersPlacesRepository
//import java.util.*
//
//class UsersPlacesService(private val userPlacesService: UsersPlacesRepository) {
//    suspend fun addUserVote(user_id: UUID, place_id: UUID) = userPlacesService.addOneRecord(user_id,place_id)
//    suspend fun getVotesForPlace(place_id: UUID) : Int = userPlacesService.getCountForPlace(place_id)
//    suspend fun getPlacesForUser(user_id: UUID): List<UUID> = userPlacesService.getPlacesForUser(user_id)
//}